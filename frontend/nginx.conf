server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Webhook ingress (Slack, Gmail)
    location /slack/ {
        proxy_pass http://webhooks:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /gmail/ {
        proxy_pass http://webhooks:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Payments service
    location /api/v1/ {
        proxy_pass http://payments:8001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Core API
    location ~ ^/(auth|companies|integrations|incidents|plans|users|subscriptions|internal|usage|employees)(/|$) {
        # NOTE: /employees is intentionally kept here — it routes to the API.
        # The React route /employees is served by the catch-all try_files below.
        proxy_pass http://api:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Frontend static files
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
